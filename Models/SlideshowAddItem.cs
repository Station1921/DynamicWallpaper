namespace DynamicWallpaper.Models
{
    /// <summary>
    /// 幻灯片页的「添加文件夹」占位卡片标记类：无任何数据，
    /// 仅用于在 SlideshowItems 集合末尾占位，由隐式 DataTemplate 渲染成添加入口卡片。
    /// 添加过文件夹后该卡片后移到集合末尾，不消失，方便继续追加文件夹。
    /// </summary>
    public class SlideshowAddItem
    {
    }
}

<Window x:Class="DynamicWallpaper.UI.AddLinkDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="添加网络壁纸" Height="260" Width="580" WindowStartupLocation="CenterOwner"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Background" Value="#4C8DFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#6BA0FF"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.6"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="GhostBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#C8C8D2"/>
            <Setter Property="BorderBrush" Value="#3A3A45"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,7"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2A2A33"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CloseBtn" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#C8C8D2"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border CornerRadius="14" Background="#1B1B22" BorderBrush="#3A3A45" BorderThickness="1" Margin="1" SnapsToDevicePixels="True">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- 标题栏：仅此处可拖拽；按钮自身处理点击，不触发拖拽 -->
            <Border Grid.Row="0" Background="#1B1B22" CornerRadius="14,14,0,0"
                    Padding="16,10" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <TextBlock Text="添加网络壁纸" Foreground="White" FontSize="13" FontWeight="SemiBold"
                               HorizontalAlignment="Left" VerticalAlignment="Center"/>
                    <Button Content="&#x2715;" x:Name="CloseBtn" Style="{StaticResource CloseBtn}"
                            HorizontalAlignment="Right" VerticalAlignment="Center" Click="Close_Click"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="18,0,18,10">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Foreground="#C8C8D2" FontSize="12" TextWrapping="Wrap" Margin="0,0,0,12">
                    <Run Text="粘贴【媒体直链】（视频/图片，通常以 .mp4 / .webm / .jpg 等结尾，不是网页地址）。"/>
                    <LineBreak/>
                    <Run Text="勾选「在线直播」则直接流播、不下载（适合 .mp4/.webm 等直链，断网/限速会卡顿，m3u8 等流媒体暂不支持）；"/>
                    <LineBreak/>
                    <Run Text="取消勾选则下载到本地。若站点用 JS 加载视频，请用浏览器 F12 → 网络 → 筛选 Media 复制真正的 .mp4 直链。"/>
                </TextBlock>

                <Border Grid.Row="1" Background="#2A2A33" BorderBrush="#3A3A45" BorderThickness="1"
                        CornerRadius="8" Padding="0" Margin="0,0,0,10">
                    <TextBox x:Name="UrlBox" Background="Transparent" Foreground="White" BorderThickness="0"
                             Padding="10,8" FontSize="12" VerticalContentAlignment="Center"
                             ToolTip="支持视频/图片直链。文件名或类型由链接与响应头推断。"
                             CaretBrush="White"/>
                </Border>

                <!-- 圆角进度条：默认隐藏，下载时显示 -->
                <Border Grid.Row="2" x:Name="ProgressBarBorder" Background="#2A2A33" BorderBrush="#3A3A45"
                        BorderThickness="1" CornerRadius="3" Padding="0" Margin="0,0,0,10"
                        Visibility="Collapsed" ClipToBounds="True">
                    <ProgressBar x:Name="ProgressBar" Height="6" Background="Transparent" Foreground="#4C8DFF"
                                   BorderThickness="0" Value="0" Minimum="0" Maximum="100"/>
                </Border>

                <Grid Grid.Row="3" Margin="0,4,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Center">
                        <CheckBox x:Name="OnlineChk" Content="在线直播（不下载）" IsChecked="True"
                                  Foreground="#C8C8D2" FontSize="12" VerticalAlignment="Center"
                                  Margin="0,0,16,0" Checked="OnlineChk_Changed" Unchecked="OnlineChk_Changed"/>
                        <CheckBox x:Name="ApplyChk" Content="添加后直接设为壁纸" IsChecked="True"
                                  Foreground="#C8C8D2" FontSize="12" VerticalAlignment="Center"
                                  Margin="0,4,0,0"/>
                    </StackPanel>
                    <TextBlock x:Name="StatusText" Grid.Column="1" Text="" Foreground="#8A8A95" FontSize="11"
                               VerticalAlignment="Center" Margin="12,0,0,0"/>
                    <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="DownloadBtn" Content="添加" Style="{StaticResource PrimaryBtn}" Click="Download_Click" Margin="0,0,8,0"/>
                        <Button x:Name="CancelBtn" Content="取消" Style="{StaticResource GhostBtn}" Click="Cancel_Click"/>
                    </StackPanel>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Window>
